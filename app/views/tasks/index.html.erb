<%# <html>
  <body> %>
<div>
  <h1> My Tasks </h1>
  <ul id="list">
    <% @tasks.each do |task| %>
      <li> <input type="checkbox"> 
      <%= link_to task.title, task_path(task) %> </li>
    <% end %>
  </ul>
</div>

  <%# </body>
</html> %>